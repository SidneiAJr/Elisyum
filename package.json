{
  "name": "elisium",
  "version": "0.1.0",
  "description": "WAF middleware for Express — rate limit, SQLi, XSS, brute force protection",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/server.ts",
    "test": "ts-node src/test.ts",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "express": ">=4.0.0"
  },
  "peerDependenciesMeta": {
    "ioredis": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.18.2",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "ioredis": "^6.0.0",
    "jest": "^29.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "keywords": [
    "express",
    "middleware",
    "security",
    "waf",
    "rate-limit",
    "xss",
    "sql-injection"
  ],
  "license": "MIT"
}
